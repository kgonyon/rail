{
  "name": "rail",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "trap 'rm -f ./*.bun-build ./.?*.bun-build' EXIT; rm -f ./*.bun-build ./.?*.bun-build; RAIL_VERSION=$(bun -p \"require('./package.json').version\") bun build --compile --minify --env=RAIL_* src/cli.ts --outfile dist/rail",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "install:local": "bun run build && ln -sf $(pwd)/dist/rail ~/.bun/bin/rail"
  },
  "dependencies": {
    "citty": "^0.2.0",
    "consola": "^3.4.2",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
